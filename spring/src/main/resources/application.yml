spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/DB_UMC
    username: root
    password: ansdjdkrkTl

  jpa:
    hibernate:
      ddl-auto: update  # 또는 update 등으로 변경 가능
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        use_sql_comments: true
        default_batch_fetch_size: 1000
